'use strict';

/**
 * Solve the given cjam problem set
 * @param  {String[]} problemCases    Array of strings representing the problem cases
 * @param  {Function} [callback]      An optional callback if that interface is preferred
 * @return {[type]}              [description]
 */
module.exports = (problemCases) => {
  const totalCases = problemCases.shift();

  return [totalCases, 'hello', 'world'];
}
